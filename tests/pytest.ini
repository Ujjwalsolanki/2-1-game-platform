[pytest]
# Configure filterwarnings to ignore the specific collection warning
filterwarnings =
    # Quote the exact warning message string for reliable matching.
    ignore:cannot collect test class '.*' because it has a __init__ constructor